info:
  name: updateContact
  type: http
  seq: 6

http:
  method: PUT
  url: http://localhost:3001/api/contacts
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
