info:
  name: createPerson
  type: http
  seq: 5

http:
  method: POST
  url: http://localhost:3001/api/contacts
  body:
    type: json
    data: |-
      {
        "name": "Robert",
        "contact": "24553-43556"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
