info:
  name: createPerson
  type: http
  seq: 5

http:
  method: POST
  url: http://localhost:3001/api/persons
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
